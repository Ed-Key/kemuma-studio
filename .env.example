# Copy to .env.local and fill in. .env.local is gitignored.
ETSY_KEYSTRING=
ETSY_SHARED_SECRET=

# Writer models. ANTHROPIC_MODEL overrides the default (claude-opus-4-8).
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=

# Bake-off challengers (step 7): keys are optional until then.
GEMINI_API_KEY=
XAI_API_KEY=
OPENAI_API_KEY=
